{
  "name": "company-api",
  "version": "1.0.0",
  "description": "JSON Server backend for company directory",
  "main": "index.js",
  "scripts": {
    "start": "cross-env PORT=$PORT json-server --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": "22.x"
  }
}
