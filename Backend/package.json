{
  "name": "company-api",
  "version": "1.0.0",
  "description": "JSON Server backend for company directory",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "engines": {
    "node": "22.x"
  }
}
