{
  "name": "company-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
